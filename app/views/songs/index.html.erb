<h1>Song List</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Duration</th>
  </tr>

  <% @songs.each do |song| %>
    <tr>
      <td><%= link_to song.name, song_path(id: song.id) %></td>
      <td><%= duration_as_minutes_and_seconds(song.duration) %></td>
    </tr>
  <% end %>

</table>
